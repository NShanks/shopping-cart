from math import floor, pi
print(floor(pi))

def area(x, y):
    area = (x * y)
    return print(f"The area is {area} square units")


def circumference(a):
    circ = (2*pi*a)
    return print(f"The circumference is {circ} units")
